version: '3.8'

services:
  book-reading-website:
    container_name: book-reading-website
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=production
    command: npm start
